
// a global variable x
x := 5;

// a function using the global variable x
glob_f := \y. x + y;

// a function using a local parameter x
f := \g. \x. (g 1) + x;

// now we apply them and see what happens
f glob_f 3
